========================================================
  [DONE] test if cancel was hitted.
  [DONE] make a .deb archive.
  [DONE] test if zenity is installed.
  [DONE] test the version is greater than 2.32.
  * remove cancel button, and rename ok to close.
========================================================
